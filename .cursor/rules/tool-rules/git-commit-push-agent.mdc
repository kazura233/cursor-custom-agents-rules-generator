---
description: 此规则管理向 git 仓库提交和推送更改的过程。在以下情况下应应用此规则：(1) 用户表示想要提交或推送更改时，(2) 用户询问 git 提交规范时，(3) 用户想要更新或保存工作到 git 时，或 (4) 请求任何 git 相关的提交和推送操作时。该规则确保一致的提交消息格式、适当的更改文档和可维护的 git 历史记录。这对于维护清晰的项目历史、促进代码审查和确保更改的正确文档记录尤为重要。此规则有助于维护高质量的提交消息，解释更改的内容和原因，使人类和 AI 都能更容易理解项目的演变。
globs:
alwaysApply: false
---

# Git 提交和推送规范

## 关键规则

- 始终从工作区根目录运行 `git add .` 来暂存更改
- 在提交前检查暂存的更改，确保不包含意外文件
- 将提交标题格式化为 `type: brief description`，其中 type 是以下之一：
  - feat: 新功能
  - fix: 错误修复
  - docs: 文档更改
  - style: 格式化、缺少分号等
  - refactor: 代码重构
  - test: 添加测试
  - chore: 维护任务
- 保持提交标题简洁明了（最多 72 个字符）
- 在提交标题后添加两个换行
- 包含详细的主体段落，解释：
  - 进行了哪些更改
  - 为什么需要进行这些更改
  - 任何重要的实现细节
- 以 " -Agent Generated Commit Message" 结束提交消息
- 将更改推送到当前远程分支

## 示例

<example>
feat: 添加用户认证系统

实现了基于 JWT 的用户认证系统，包括安全的密码哈希
和令牌刷新功能。此更改提供了安全的用户会话
并防止对受保护路由的未授权访问。实现
使用 bcrypt 进行密码哈希，并包含适当的令牌过期处理。

-Agent Generated Commit Message
</example>

<example type="invalid">
更新了一些内容

修复了一些错误并添加了功能
</example>
